#
# PAM configuration file for the swaylock screen locker. By default, it includes
# the 'login' configuration file (see /etc/pam.d/login)
#

# Added yubikey u2f by swaygo
auth sufficient   pam_u2f.so cue
auth include      login
